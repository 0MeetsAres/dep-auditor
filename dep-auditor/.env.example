# dep-auditor MCP Server - Configuration
# No API keys required for basic operation (uses public registries + OSV.dev)
# Add keys below if rate-limited:

# NPM_TOKEN=                  # Optional: npm registry token for higher rate limits
# GITHUB_TOKEN=               # Optional: for GitHub Advisory Database lookups
